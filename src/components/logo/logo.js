import React from 'react';
import styles from './logo.module.scss';

const Logo = () => {
  return (
    <div>
      <h1 className={styles.h1}>CONTACTIFY</h1>
    </div>
  );
};

export default Logo;
